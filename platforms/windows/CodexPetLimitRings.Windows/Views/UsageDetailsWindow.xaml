<Window x:Class="CodexPetLimitRings.Windows.Views.UsageDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Codex 포션 상세" Width="340" MinHeight="285" MaxHeight="600" SizeToContent="Height" ResizeMode="NoResize"
        WindowStyle="ToolWindow" ShowInTaskbar="False" Topmost="True"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"
        FontFamily="Segoe UI, Malgun Gothic" TextOptions.TextFormattingMode="Display"
        Closing="Window_OnClosing">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <StackPanel>
      <StackPanel Orientation="Horizontal">
        <TextBlock Text="🧪" FontSize="14" Margin="0,0,5,0" />
        <TextBlock Text="Codex 포션 상세" FontSize="15" FontWeight="Bold" />
      </StackPanel>
      <TextBlock Text="실제 Codex 펫의 두 사용 한도" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" FontSize="11" />
    </StackPanel>
    <Border Grid.Row="1" Margin="0,12,0,5" Padding="10" CornerRadius="9"
            BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="1"
            Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
      <StackPanel>
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="●" Foreground="#DC2626" FontSize="11" Margin="0,0,5,0" />
          <TextBlock Text="5시간 포션" FontSize="12" FontWeight="SemiBold" />
        </StackPanel>
        <TextBlock x:Name="PrimaryValue" FontSize="18" FontWeight="Bold" Typography.NumeralAlignment="Tabular" />
        <TextBlock x:Name="PrimaryReset" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" FontSize="10.5" />
      </StackPanel>
    </Border>
    <Border Grid.Row="2" Margin="0,5,0,12" Padding="10" CornerRadius="9"
            BorderBrush="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}" BorderThickness="1"
            Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
      <StackPanel>
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="●" Foreground="#2563EB" FontSize="11" Margin="0,0,5,0" />
          <TextBlock Text="주간 포션" FontSize="12" FontWeight="SemiBold" />
        </StackPanel>
        <TextBlock x:Name="SecondaryValue" FontSize="18" FontWeight="Bold" Typography.NumeralAlignment="Tabular" />
        <TextBlock x:Name="SecondaryReset" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" FontSize="10.5" />
      </StackPanel>
    </Border>
    <DockPanel Grid.Row="3">
      <TextBlock x:Name="SourceText" Foreground="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" FontSize="10.5" VerticalAlignment="Center" />
      <Button x:Name="RefreshButton" DockPanel.Dock="Right" Content="지금 갱신" FontSize="11" Padding="10,3" Click="RefreshButton_OnClick" />
    </DockPanel>
  </Grid>
</Window>
